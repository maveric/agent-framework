# Kanban Board Full-Stack Infrastructure Plan (Task 636)

## Component Overview
Establish foundational full-stack setup for Kanban board: Flask backend, SQLite 'kanban.db' with 'tasks' table, single-page HTML/CSS/JS frontend. Strictly per task acceptance criteria and design_spec.md architecture. No APIs, UI logic, or extras.

## Granularity
Commit-level: atomic, reviewable changes with inline verification.

## Tasks Overview
1. Project init + deps
2. Flask app skeleton
3. DB schema + init
4. Frontend scaffold
5. Basic verification test

## Dependencies
Sequential: structure → Flask → DB → frontend → test.

Follow design_spec.md strictly - no scope expansion.
