# Observability Environment Configuration

# API Server Configuration
# This is the IP/hostname where your Agent Orchestrator API is running
# that Prometheus should scrape metrics from

# For localhost development:
API_HOST=localhost
API_PORT=8085  # Default server port (matches src/server.py)

# For VM deployment (replace with your VM's IP):
# API_HOST=192.168.1.100
# API_PORT=8085

# For Docker network (if API is also in Docker):
# API_HOST=host.docker.internal  # Mac/Windows
# API_HOST=172.17.0.1             # Linux (Docker bridge)

# Grafana Configuration
GRAFANA_PORT=3001
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# Prometheus Configuration
PROMETHEUS_PORT=9090
PROMETHEUS_SCRAPE_INTERVAL=15s
